# -*- coding: utf-8 -*-
"""ОБРАЗЕЦ конфигурации языка. Скопировать в <lang>.py и заполнить.

Это не рабочий файл: механика его не читает (имя начинается с подчёркивания).
Здесь перечислены ВСЕ поля, которые понимает audit_lang.py, с пояснением, чем
грозит неверный выбор. Обязательных полей нет: чего язык не объявил, то не
проверяется - и это опасно ровно тем, что отчёт при этом говорит «чисто».
Поэтому заполнять сверху вниз и вычёркивать явно, а не молчанием.

Порядок работы на новом языке:
  1. Скопировать этот файл в audit_conf/<lang>.py.
  2. Заполнить блок «письмо и счёт» - он определяет ВСЁ остальное.
  3. GLOSSARY заполняется из словаря решений языка, разделы 2 и 3, а не
     наоборот: сначала решение с ссылкой на акт, потом строка здесь.
  4. HARD_LOCKS и SOFT_LOCKS - из раздела 7 словаря решений.
  5. Запустить `python _tools/audit_lang.py <lang>` и разобрать КАЖДОЕ
     срабатывание. Ложное - снимать не из отчёта, а из этого файла, с
     комментарием почему. Отчёт, где половина строк ложные, перестают читать.

Вспомогательные величины внутри конфигурации называются с подчёркивания -
иначе механика примет их за опечатку в имени поля и остановится.
"""
from . import _helpers as H            # noqa: F401  (образец, импорт для примера

LANG = 'xx'
TITLE = 'название языка по-русски'

# --------------------------------------------------------------------------
# Письмо и счёт. Заполняется ПЕРВЫМ: от этого зависит, работают ли остальные
# разделы вообще.
# --------------------------------------------------------------------------

# 'word' - формы почти не меняются (английский);
# 'flex' - основа плюс короткое окончание (испанский, французский, немецкий);
# 'stem' - основа плюс длинное окончание (грузинский и всё агглютинативное);
# 'sub'  - счёт вхождений подстроки: письменность БЕЗ ПРОБЕЛОВ (китайский).
#          Там не годится ни одна из трёх первых: границы слова \b нет нигде,
#          потому что соседний иероглиф - тоже \w, а окончаний не существует.
# Ошибка здесь тихая: 'word' на флективном языке считает треть вхождений и
# показывает «чисто» там, где термин расползся.
#
# 'sub' меняет ещё три механизма, и все три - в audit_lang.py, а не здесь:
# предложения режутся без обязательного пробела после точки, длина
# предложения меряется знаками письма вместо split(), повтор слова ищется
# вплотную и без границ. Обходить их в конфигурации нельзя - каждый из них
# при пробельных настройках даёт не ошибку, а пустой результат и «чисто».
COUNT = 'flex'
FLEX_EXTRA = 3          # допуск окончания для 'flex'
STEM_EXTRA = 8          # допуск окончания для 'stem'
LETTERS = H.LATIN       # класс букв письма; для ka - H.MKHEDRULI, zh - H.CJK

# Считать термины без учёта регистра. Для романских - да. Для немецкого НЕТ:
# заглавная там различает слова. Для письменностей без регистра неважно.
COUNT_CI = True

# На каком тексте искать расползание:
#   'clean' - без дословных цитат: язык официального ПЕРЕВОДА акта, где текст
#             акта законно говорит своим словом (грузинский);
#   'raw'   - на всём: язык АУТЕНТИЧНОГО текста, где расхождение внутри
#             цитаты есть настоящий сигнал (английский, испанский,
#             французский; немецкий - официальный перевод, но термины сверены).
GLOSSARY_ON = 'raw'

# --------------------------------------------------------------------------
# 1. Варианты одного слова
# --------------------------------------------------------------------------
PAIRS_TITLE = 'ОРФОГРАФИЧЕСКИЕ ВАРИАНТЫ'
# 'exact' - строго по границе слова; 'term' - по модели COUNT.
# При COUNT='sub' обязателен 'term': граница слова на письменности без
# пробелов не находит ничего, и механика на этом останавливается ассертом.
PAIRS_COUNT = 'exact'
PAIRS = [
    # ('норма корпуса', 'конкурент'),
]

# --------------------------------------------------------------------------
# 2. Обороты-тики: верны поодиночке, выдают перевод, когда размазаны
# --------------------------------------------------------------------------
TICS = []
TIC_MIN = 10
TICS_CI = False         # True там, где оборот часто открывает предложение

# --------------------------------------------------------------------------
# 3. Расползание терминов. Ключ - русское понятие, первый вариант - норма
#    корпуса, остальные конкуренты. Источник: <lang>/_СЛОВАРЬ_РЕШЕНИЙ.md.
# --------------------------------------------------------------------------
GLOSSARY = {}

# Переопределение счёта для отдельного варианта: {вариант: функция(текст)}.
# Нужно там, где слово прячется внутри имени собственного - грузинское ერი
# внутри имени ООН.
COUNT_OVERRIDE = {}

# --------------------------------------------------------------------------
# 4. Кальки: грамматически верно и выдаёт русский исходник
# --------------------------------------------------------------------------
CALQUES = [
    # (r'регулярное выражение', 'чем заменить'),
]

# --------------------------------------------------------------------------
# 5. Механика набора
# --------------------------------------------------------------------------
# Знаки, перед которыми пробел есть дефект. Во ФРАНЦУЗСКОМ перед ; : ! ?
# пробел - НОРМА, и там здесь только ',.'. Перепутать значит объявить норму
# языка ошибкой.
SPACE_BEFORE_PUNCT = ',.;:!?'
REPEAT_LETTERS = None   # класс букв для «повтор слова»; None - проверка выкл
REPEAT_MIN = 3
REPEAT_SKIP = ()        # слова, законно идущие подряд (нем. «die die»)

# --------------------------------------------------------------------------
# 6. Типографика
# --------------------------------------------------------------------------
QUOTES = (H.GUILLEMET_OPEN, H.GUILLEMET_CLOSE)   # нем.: H.LOW9_OPEN/CLOSE
QUOTES_NAME = 'кавычки'
SPACE_IN_QUOTES = False   # True только для французского: « texte » - норма
ALLOWED = set()           # кодпойнты, законные в этом языке (нем.: низкие кав.)
EXTRA_BAD = {}            # запрещённые дополнительно (нем.: ёлочки)
PAIRED = []               # [(знак, парный, подпись)] - испанские ¿ ¡

# --------------------------------------------------------------------------
# 7-9
# --------------------------------------------------------------------------
FORBIDDEN_WORDS = []      # переводы имени народа: Erdling, Terrien, terricola
CAPACITY = []             # правоспособность / дееспособность / правосубъектность
CAPACITY_TITLE = 'ПРАВОСПОСОБНОСТЬ, ДЕЕСПОСОБНОСТЬ, ПРАВОСУБЪЕКТНОСТЬ'
LONG_SENT = 60            # немецкий держит 70, романские 60
SENT_START = None         # класс знаков начала фразы; None - собрать из LETTERS
# Знаки конца фразы. У письменности со своей пунктуацией они свои: китайская
# точка - это 。, и без неё файл считается одним предложением, а раздел 9
# печатает «чисто» на непроверенном тексте.
SENT_END = '.!?'

# --------------------------------------------------------------------------
# 10. Замки из раздела 7 словаря решений
# --------------------------------------------------------------------------
# HARD - законного вхождения не бывает.
HARD_LOCKS = [
    # (r'выражение', 'почему нельзя'),
]
# SOFT - бывает. Третий элемент: регулярное выражение ЗАКОННОГО окружения;
# если оно есть в 60 знаках вокруг находки, находка не печатается. Без него
# отчёт состоит из ложных строк - на грузинском фильтр снял 55 из 55.
SOFT_LOCKS = [
    # (r'выражение', 'когда законно', r'законное окружение|или None'),
]

# --------------------------------------------------------------------------
# 11. Цитаты на сверку с актом
# --------------------------------------------------------------------------
QUOTE_SRC = None          # признак абзаца с цитатой из акта
SOURCE_NOTE = 'каждую сверить с текстом акта'

# --------------------------------------------------------------------------
# Свои разделы языка: то, чего нет ни у кого больше.
# Функция получает ctx с полями texts, clean, allw, allc, conf.
# --------------------------------------------------------------------------
EXTRA_SECTIONS = []
